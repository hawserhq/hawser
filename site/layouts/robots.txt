{{/* Hugo's built-in robots.txt is "User-agent: *" and nothing else, which
     leaves the sitemap undiscoverable: it exists, but nothing announces it.
     This is the one line that points a crawler at it without a Search Console
     submission. Everything here is public documentation, so nothing is
     disallowed. */ -}}
User-agent: *
Allow: /

Sitemap: {{ "sitemap.xml" | absURL }}
